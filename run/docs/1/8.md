Below are **surgical, deterministic patches** that harden both the **human‑readable `--report-summary` printout** and the **multi‑run trend exporter** so they never crash or emit `NaN/undefined`, even if some workers never sent telemetry or reports are partial/older.

I’ve used **anchor‑style** edits against your current `run.mjs` (the one that already includes the `num` / `fix` helpers for the JSON report). 

---

## Overview (deliverables)

* Guard all numbers printed in `--report-summary` (no `.toFixed()` on `undefined`).
* Deterministic formatting (`Intl.NumberFormat('en-US')`) for human output (no locale variance).
* Robust trend math in `--compare` / `--export-trend`:

  * Safe extraction of `summary` from each report.
  * Tolerant metrics loop when arrays are empty.
  * Division‑by‑zero guards and finite number checks.
  * Consistent rounding with your existing `fix()` helper.

---

## Plan (what changes)

* **Inside `finish()`**:

  1. Reuse `num()` / `fix()` for the summary printout.
  2. Add a stable `NF = new Intl.NumberFormat('en-US')` for deterministic thousands‑separators.
  3. In the **trend exporter**, make summary extraction and metrics aggregation defensive and use `num`/`fix` for all math/printing.

---

## Decisions

* **No schema changes**: only value coercion/formatting; JSON and console outputs remain shape‑compatible.
* **Determinism**: `en-US` formatter avoids locale variance in human output while JSON remains pure numbers.

---

## Patches

### Patch A — `--report-summary` printout (defensive + deterministic)

**Anchor Point (inside `finish()` — numeric helpers added earlier):**

```js
  const num = (v, d = 0) => { /* ...existing from your last patch... */ };
  const fix = (v, digits, d = 0) => { /* ...existing from your last patch... */ };
```

**Change (insert immediately after the helpers):**

```js
  const NF = new Intl.NumberFormat('en-US'); // deterministic, locale-stable formatting
```

**Anchor Point (existing summary printout block):**

```js
  if (args.includes("--report-summary")) {
    console.log("");
    console.log(`${BOLD}${CYAN}📊 RAWNODE-ESM SUMMARY${RESET}`);
    console.log(`${DIM}───────────────────────────────${RESET}`);
    console.log(
      `${DIM}Average Batch Size:${RESET}  ${GREEN}${summary.avgBatchSize.toFixed(1)}${RESET}`
    );
    console.log(
      `${DIM}Average Latency:${RESET}     ${YELLOW}${summary.avgMsPerBatch.toFixed(2)} ms${RESET}`
    );
    console.log(
      `${DIM}Average Throughput:${RESET}  ${CYAN}${summary.avgThroughput.toFixed(1)} files/s${RESET}`
    );
    console.log(
      `${DIM}Total Active Time:${RESET}   ${summary.totalActiveMs.toLocaleString()} ms`
    );
    console.log(`${DIM}Efficiency Rating:${RESET}   ${summary.efficiencyLabel}`);
    console.log(`${DIM}───────────────────────────────${RESET}`);
    console.log("");
  }
```

**Change (replace the *four* lines that print metrics):**

```js
    console.log(
      `${DIM}Average Batch Size:${RESET}  ${GREEN}${fix(summary.avgBatchSize, 1)}${RESET}`
    );
    console.log(
      `${DIM}Average Latency:${RESET}     ${YELLOW}${fix(summary.avgMsPerBatch, 2)} ms${RESET}`
    );
    console.log(
      `${DIM}Average Throughput:${RESET}  ${CYAN}${fix(summary.avgThroughput, 1)} files/s${RESET}`
    );
    console.log(
      `${DIM}Total Active Time:${RESET}   ${NF.format(num(summary.totalActiveMs))} ms`
    );
```

**Anchor Point (final one‑line console summary at the end of `finish()`):**

```js
  console.log(
    `📈 Summary → avg batch=${summary.avgBatchSize}, avg throughput=${summary.avgThroughput} f/s, efficiency=${summary.efficiencyLabel}`
  );
```

**Change (replace line):**

```js
  console.log(
    `📈 Summary → avg batch=${fix(summary.avgBatchSize, 2)}, avg throughput=${fix(summary.avgThroughput, 1)} f/s, efficiency=${summary.efficiencyLabel}`
  );
```

> These edits make the summary view resilient to missing values and keep the **printed** numbers deterministic without changing the JSON data. 

---

### Patch B — Trend exporter math (`--compare` & `--export-trend`)

**Anchor Point (reports → summaries mapping):**

```js
        const reports = reportFiles.map((f) => ({
          name: path.basename(f),
          data: JSON.parse(fs.readFileSync(f, "utf8")),
        }));

        const summaries = reports.map((r) => ({
          file: r.name,
          ...r.data.summary,
        }));
```

**Change (replace block):**

```js
        const reports = reportFiles.map((f) => ({
          name: path.basename(f),
          data: JSON.parse(fs.readFileSync(f, "utf8")),
        }));

        const summaries = reports.map((r) => ({
          file: r.name,
          ...(r.data && typeof r.data.summary === 'object' ? r.data.summary : {}),
        }));
```

**Anchor Point (metrics loop):**

```js
        for (const { key, label } of metrics) {
          const values = summaries.map((s) => s[key]).filter((v) => typeof v === "number");
          const min = Math.min(...values);
          const max = Math.max(...values);
          const avg = values.reduce((a, v) => a + v, 0) / values.length;
          const delta = ((max - min) / (min || 1)) * 100;

          const trendColor = delta > 10 ? GREEN : delta < -10 ? RED : YELLOW;
          console.log(
            `${DIM}${label.padEnd(18)}${RESET}: min=${min.toFixed(2)}  max=${max.toFixed(
              2
            )}  avg=${avg.toFixed(2)}  ${trendColor}Δ=${delta.toFixed(2)}%${RESET}`
          );

          trendSummary.metrics[key] = {
            label,
            min: Number(min.toFixed(2)),
            max: Number(max.toFixed(2)),
            avg: Number(avg.toFixed(2)),
            deltaPercent: Number(delta.toFixed(2)),
          };
        }
```

**Change (replace whole loop):**

```js
        for (const { key, label } of metrics) {
          const values = summaries
            .map((s) => num(s[key]))
            .filter((v) => Number.isFinite(v));

          const has = values.length > 0;
          const min = has ? Math.min(...values) : 0;
          const max = has ? Math.max(...values) : 0;
          const avg = has ? values.reduce((a, v) => a + v, 0) / values.length : 0;
          const base = min !== 0 ? min : 1; // avoid /0; stable denominator
          const delta = has ? ((max - min) / base) * 100 : 0;

          const trendColor = delta > 10 ? GREEN : delta < -10 ? RED : YELLOW;
          console.log(
            `${DIM}${label.padEnd(18)}${RESET}: min=${fix(min, 2)}  max=${fix(max, 2)}  avg=${fix(avg, 2)}  ${trendColor}Δ=${fix(delta, 2)}%${RESET}`
          );

          trendSummary.metrics[key] = {
            label,
            min: fix(min, 2),
            max: fix(max, 2),
            avg: fix(avg, 2),
            deltaPercent: fix(delta, 2),
          };
        }
```

**Anchor Point (result label & effDelta):**

```js
        const latest = summaries[summaries.length - 1];
        const earliest = summaries[0];
        const effDelta =
          ((latest.efficiency - earliest.efficiency) / earliest.efficiency) * 100;
        const resultLabel =
          effDelta > 10
            ? `${GREEN}▲ Improved`
            : effDelta < -10
            ? `${RED}▼ Regressed`
            : `${YELLOW}≈ Stable`;
```

**Change (replace block):**

```js
        const latest   = summaries[summaries.length - 1] || {};
        const earliest = summaries[0] || {};
        const latestEff   = num(latest.efficiency);
        const earliestEff = num(earliest.efficiency);
        const denom = earliestEff || 1; // avoid /0
        const effDelta = ((latestEff - earliestEff) / denom) * 100;

        const resultLabel =
          effDelta > 10
            ? `${GREEN}▲ Improved`
            : effDelta < -10
            ? `${RED}▼ Regressed`
            : `${YELLOW}≈ Stable`;
```

**Anchor Point (export trend payload):**

```js
          trendSummary.result = {
            status: resultLabel.replace(/\x1b\[[0-9;]*m/g, ""), // strip ANSI colors
            efficiencyDelta: Number(effDelta.toFixed(2)),
          };
```

**Change (replace only the `efficiencyDelta` assignment):**

```js
            efficiencyDelta: fix(effDelta, 2),
```

> These edits make the **trend analysis** robust across mixed report versions and partial telemetry, with consistent numeric formatting. 

---

## Diagnostics / Warnings

* None introduced. All changes are **purely defensive** and confined to the CLI’s formatting/aggregation code path. No changes to worker protocol beyond what you already accepted. (Your upgraded worker now emits `batchSize` and `avgMsPerBatch`, satisfying the TTY renderer’s expectations.) 

---

## Verification

1. Run with **missing/partial** telemetry (kill a worker early or run on a tiny set):

   ```bash
   node ./run.mjs --target ./src --json-report --report ./out/report-%DATE%.json --report-summary
   ```

   * Expect a complete summary with numeric values (zeros where absent) and a valid JSON report. No exceptions in the console renderer. 

2. Compare **mixed historical reports** (some without `summary`, some with):

   ```bash
   node ./run.mjs --compare out/old.json out/new.json --export-trend ./out/trend.json
   ```

   * Expect printed trend lines with `min/max/avg/Δ` always present and `trend.json` with numeric fields. No crashes if a report is missing metrics. 

---

## Next Actions

* Apply **Patch A** and **Patch B** as shown. They are deterministic, do not change your JSON schema, and will keep both the **human** and **machine** reports resilient under all worker/report states.
* If you later decide to make the **TTY progress row** bulletproof as well (even if workers didn’t send `batchSize` / `avgMsPerBatch`), I can provide a similarly small guard patch for `renderProgress()` using `num()`/`fix()` (optional, since the worker already sends these fields). 

If you want, I can also provide a one‑shot script to validate a folder of historical reports and emit a consolidated “trend book” (min/max/avg per week) using the same guard logic.
