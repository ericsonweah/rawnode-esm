

## TASK — UPGRADE `run.js` FOR RAWNODE‑ESM

**Role**
Act as a principal Node.js compiler + performance engineer. You are enhancing **rawnode‑esm**’s main runner/orchestrator (`run.js` / `run.mjs`). The project is a **zero‑dependency**, **build‑free**, **deterministic**, **non‑blocking** CommonJS→ESM conversion engine built **only on Node core APIs**.

**Context**
`run.js` works today, but I want it **next‑level**: feature‑rich, advanced, fast, extensible (hooks/plugins), developer‑friendly, and strictly non‑blocking of the event loop. It must convert entire codebases (e.g., RawNode’s backend), handle *real‑world* patterns beyond existing tools, and remain deterministic without external packages or AST toolchains.

---

### Hard Constraints (non‑negotiable)

1. **Zero deps**: Use only Node core (`fs/promises`, `path`, `url`, `module`, `worker_threads`, `perf_hooks`, `crypto`, `os`, `readline`, `vm`). No Babel/Recast/Esprima/Acorn/TS‑Morph/Prettier, no bundlers, no third‑party CLIs.
2. **Node 20+**; prefer `node:` specifiers.
3. **Build‑free**: runnable via `node ./bin/rawnode-esm.mjs`.
4. **Deterministic**: same inputs ⇒ byte‑identical outputs; stable sorting; normalized paths; no time/locale randomness.
5. **Non‑blocking**: async I/O; CPU‑heavy work in `worker_threads` with bounded concurrency + back‑pressure.
6. **Surgical edits** only: preserve formatting & comments; never wholesale reformat.
7. **Safety first**: when ambiguous, choose safe interop or emit a precise diagnostic—never silently change semantics.

---

### Required Capabilities (that `run.js` must orchestrate)

* **Whole‑repo conversion** with include/exclude globs and **incremental** mode via content‑hash cache.
* **Real‑world patterns** handled conservatively and deterministically:

  * Auto‑detect `require()` scope (top‑level vs local).
  * Directory vs file import resolution via **stat cache** + `package.json` `exports`/`main`.
  * Preserve comments/formatting (string/regex/template‑aware replacements).
  * Conditional/branched/try‑catch requires → guarded `import()` or `createRequire` fallback.
  * `module.exports` / `exports.*` → `export default` / named exports.
  * `__dirname`/`__filename` → `import.meta.url` + `fileURLToPath`.
  * `require.resolve()` → `createRequire(import.meta.url).resolve()`.
  * Side‑effect requires → `import 'pkg/register'`.
  * JSON/WASM/other assets via `createRequire` or explicit interop.
  * Mixed ESM/CJS and circular deps: convert safely, inject shims when needed.

---

### Architecture you should (re)implement in `run.js`

* **Pipeline**: `discover → analyze → plan → transform → verify → write → report`.
* **Workers**: a bounded **worker_threads** pool for scanning/analysis & transformation (configurable concurrency).
* **Back‑pressure**: queue with high‑water/low‑water marks; do not starve the event loop.
* **Cache**: on‑disk content‑hash + config key; incremental runs skip unchanged files.
* **Stat cache & resolver**: normalize specifiers; distinguish file vs directory; prefer `node:` for core.
* **Deterministic execution**: lexicographic stable file order; deterministic plugin order; no Date/Math.random in plans.
* **Abortability**: accept `AbortSignal` + `timeout` for graceful cancellation.
* **Config intake**: merge CLI flags and optional `rawnode-esm.config.mjs` (core APIs only).
* **Observability**: structured events + timing via `perf_hooks.performance`.

---

### Hooks/Plugin System (no deps)

* **Lifecycle hooks** (all optional):
  `onDiscover(files)`, `onAnalyze(file, facts)`, `onPlan(file, plan)`, `onTransform(file, edits)`,
  `onVerify(file, report)`, `onWrite(file, result)`, `onEnd(summary)`.
* **Context (`ctx`)** provided to plugins:
  `read/write`, `statCache`, `resolveSpecifier`, `logger`, `metrics`,
  `riskProfile`, `createRequireShim()`, `addWarning()`, `registerFileDependency()`.
* **Determinism**: explicit plugin order, serial within file, no time‑based behavior.

---

### CLI & Programmatic API (to be implemented or upgraded)

* **CLI**:
  `rawnode-esm [paths...] --dry-run --check --fix --risk safe|aggressive --concurrency N --include "<glob>" --exclude "<glob>" --plugins "./plugins/*.mjs" --report json|pretty --print-diff --cache-dir ".rawnode-esm" --timeout <ms>`
* **API**:

  ```js
  import { convert } from 'rawnode-esm';
  await convert({
    roots: ['src','lib'],
    include: ['**/*.js','**/*.cjs'],
    exclude: ['node_modules/**','dist/**'],
    risk: 'safe', concurrency: Math.min(8, os.cpus().length),
    dryRun: false, check: false, timeout: 120000,
    plugins: [myPlugin()],
    signal: abortController.signal,
    onProgress: (evt) => {}
  });
  ```

---

### Output I Expect from You (single response)

1. **Overview**: what you will change in `run.js` and why.
2. **Architecture blueprint**: module boundaries, data flow, worker model, back‑pressure, cache layout.
3. **Hook/Plugin spec**: API surface, lifecycle order, determinism guarantees.
4. **CLI & API docs**: flags, defaults, exit codes, examples.
5. **Instrumentation**: progress events schema, metrics, timing table, sample pretty and JSON report.
6. **Risk policy**: CJS→ESM mapping rules (safe vs aggressive), with fallbacks & diagnostics.
7. **Code**: a **drop‑in improved `run.mjs`** (ESM) plus minimal supporting stubs (`discover.mjs`, `scan.mjs`, `plan.mjs`, `transform.mjs`, `verify.mjs`, `observability.mjs`, `worker.mjs`, `cache.mjs`). Use only Node core, ESM syntax, and `node:` specifiers.
8. **Diffs**: if I paste my current `run.js`, output a minimal **unified diff**. If I don’t, output the full file.
9. **Tests/fixtures**: at least 10 before/after fixtures + a smoke test script; explain how to run them with plain Node.
10. **Performance plan**: worker sizing, queue thresholds, expected LOC/s, and memory notes.
11. **Assumptions & limitations**: call out edges you conservatively skip and the warning codes emitted.

> **Important authoring rules for your answer**
>
> * Do not ask me questions; choose safe defaults and proceed.
> * Keep code runnable on Node 20+ with **no external dependencies**.
> * Avoid global reformatting; perform minimal, position‑preserving edits.
> * Use crisp headings, bullet lists, and short code blocks.
> * Prefer examples that compile; include `node:` specifiers and `import.meta.url` idioms.
> * Provide actionable diagnostics with `{file,line,col,code,message,hint}`.

---

### Canonical snippets to reuse

**__dirname/__filename**

```js
import { fileURLToPath } from 'node:url';
import { dirname } from 'node:path';
const __filename = fileURLToPath(import.meta.url);
const __dirname  = dirname(__filename);
```

**require.resolve**

```js
import { createRequire } from 'node:module';
const require = createRequire(import.meta.url);
const p = require.resolve('pkg/subpath');
```

**Side‑effect require**

```js
// require('dotenv/config') →
import 'dotenv/config';
```

---

### Acceptance Criteria (Definition of Done)

* `run.mjs` orchestrates the pipeline with a bounded worker pool, back‑pressure, and deterministic ordering.
* Zero external deps; `npm ls` shows none.
* `--check` stable; `--fix` yields compilable ESM; comments & formatting preserved.
* Structured progress/metrics are emitted; `--report json|pretty` both work.
* Plugin example runs and demonstrates determinism.
* Fixture suite passes; warnings are actionable and deduplicated.

---

**Deliver everything in one message**. If any item can’t be completed, provide the best safe partial with clear TODOs and diagnostics.


Below is the current working implementation of run.mjs: 



'use strict';

// src/run.mjs
import fs from "node:fs";
import path from "node:path";

/* ---------- helpers ------------------------------------------------------- */

function resolveImportPath(mod, baseDir) {
  const isRelative = mod.startsWith("./") || mod.startsWith("../");
  if (!isRelative) return mod;                       // core / package
  const absPath = path.resolve(baseDir, mod);
  try {
    const stat = fs.statSync(absPath);
    if (stat.isDirectory()) return `${mod.replace(/\/$/, "")}/index.js`;
  } catch {}
  return mod.endsWith(".js") ? mod : `${mod}.js`;
}

/* ---------- main ---------------------------------------------------------- */

function replaceRequire(dir) {
  for (const file of fs.readdirSync(dir)) {
    const full = path.join(dir, file);
    const stat = fs.statSync(full);

    if (stat.isDirectory()) {
      replaceRequire(full);
      continue;
    }
    if (!file.endsWith(".js")) continue;

    let code = fs.readFileSync(full, "utf8");
    const original = code;
    const baseDir = path.dirname(full);

    /* ---- requires ------------------------------------------------------- */

    // require('x')(args)
    code = code.replace(
      /const\s+(\w+)\s*=\s*require\(['"]([^'"]+)['"]\)\(([^)]*)\);?/g,
      (_, name, mod, args) => {
        const importPath = resolveImportPath(mod, baseDir);
        return `import tmp_${name} from "${importPath}";\nconst ${name} = tmp_${name}(${args});`;
      }
    );

    // require('x').member
    code = code.replace(
      /const\s+(\w+)\s*=\s*require\(['"]([^'"]+)['"]\)\.([A-Za-z$_][\w$]*)/g,
      (_, name, mod, member) => {
        const importPath = resolveImportPath(mod, baseDir);
        return `import * as __tmp_${name} from "${importPath}";\nconst ${name} = __tmp_${name}.${member};`;
      }
    );

    // const foo = require('x')
    code = code.replace(
      /const\s+(\w+)\s*=\s*require\(['"]([^'"]+)['"]\);?/g,
      (_, name, mod) => {
        const importPath = resolveImportPath(mod, baseDir);
        return `import ${name} from "${importPath}";`;
      }
    );

    // const { a,b } = require('x')
    code = code.replace(
      /const\s*\{\s*([^}]+)\s*\}\s*=\s*require\(['"]([^'"]+)['"]\);?/g,
      (_, names, mod) => {
        const importPath = resolveImportPath(mod, baseDir);
        return `import { ${names.trim()} } from "${importPath}";`;
      }
    );

    /* ---- exports -------------------------------------------------------- */

    // Case: module.exports = something
    // keep a marker so we can later append named exports if found
    let hasDefault = false;
    code = code.replace(/module\.exports\s*=\s*([^;]+)/g, (_, rhs) => {
      hasDefault = true;
      return `export default ${rhs}`;
    });

    // Case: exports.foo = ...
    code = code.replace(
      /\bexports\.([A-Za-z$_][\w$]*)\s*=\s*(?!require)([^;\n]+)/g,
      (_, key, rhs) => `export const ${key} = ${rhs}`
    );

    // Case: module.exports.foo = ...
    code = code.replace(
      /\bmodule\.exports\.([A-Za-z$_][\w$]*)\s*=\s*([^;\n]+)/g,
      (_, key, rhs) => (hasDefault ? `export { ${rhs} as ${key} };` : `export const ${key} = ${rhs}`)
    );

    /* ---- dynamic require flags ----------------------------------------- */
    code = code.replace(
      /\brequire\(([^)]+)\)/g,
      (m) => `/* TODO dynamic require → await import(${m.slice(8, -1)}.js) */ ${m}`
    );

    if (code !== original) {
      fs.writeFileSync(full, code, "utf8");
      console.log("✅ Converted", full);
    }
  }
}

/* ------------------------------------------------------------------------ */

replaceRequire("./src");
console.log("\n🎉 Conversion complete (handles exports.*, module.exports.*, and directory index.js).");


